const _presentations = [
    { title: 'Default' },
    { 'title': 'Default1' },
    { 'title': 'Default2' },
    { 'title': 'Default3' },
    { 'title': 'Default4' }
];

export default {
    getList (cb: any) {
        let result: any;
        //result = new Promise((resolve, reject) => {
        //    setTimeout(() => resolve(_presentations), 500)
        //})

        return result;
    },
  }